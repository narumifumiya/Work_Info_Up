<% if project.favorited_by?(current_user) %>
  <p>
    <%= link_to company_project_favorites_path(company, project), method: :delete, remote: true do %>
      ♥<%= project.favorites.count %> いいね
    <% end %>
  </p>
<% else %>
  <p>
    <%= link_to company_project_favorites_path(company, project), method: :post, remote: true do %>
      ♡<%= project.favorites.count %> いいね
    <% end %>
  </p>
<% end %>